<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Test Results - OAuth 2.0 Test Client</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="card">
                    <div class="card-header">
                        <h1 class="h3 mb-0">API テスト結果</h1>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-success">
                            <h4>✅ API テスト成功!</h4>
                            <p>アクセストークンを使用してAuthorization ServerのIntrospection APIを呼び出しました。</p>
                        </div>

                        <h5>使用したアクセストークン</h5>
                        <div class="mb-3">
                            <textarea class="form-control" rows="2" readonly><%= accessToken %></textarea>
                        </div>

                        <h5>Token Introspection レスポンス</h5>
                        <div class="mb-3">
                            <pre class="bg-light p-3 rounded"><code><%= JSON.stringify(introspectionData, null, 2) %></code></pre>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <h6>トークン情報</h6>
                                <ul class="list-group list-group-flush">
                                    <li class="list-group-item">
                                        <strong>アクティブ:</strong> 
                                        <span class="badge <%= introspectionData.active ? 'bg-success' : 'bg-danger' %>">
                                            <%= introspectionData.active ? 'Yes' : 'No' %>
                                        </span>
                                    </li>
                                    <% if (introspectionData.client_id) { %>
                                        <li class="list-group-item"><strong>クライアントID:</strong> <%= introspectionData.client_id %></li>
                                    <% } %>
                                    <% if (introspectionData.scope) { %>
                                        <li class="list-group-item"><strong>スコープ:</strong> <%= introspectionData.scope %></li>
                                    <% } %>
                                    <% if (introspectionData.sub) { %>
                                        <li class="list-group-item"><strong>サブジェクト:</strong> <%= introspectionData.sub %></li>
                                    <% } %>
                                    <% if (introspectionData.exp) { %>
                                        <li class="list-group-item">
                                            <strong>有効期限:</strong> 
                                            <%= new Date(introspectionData.exp * 1000).toLocaleString('ja-JP') %>
                                        </li>
                                    <% } %>
                                </ul>
                            </div>
                            <div class="col-md-6">
                                <h6>テスト結果</h6>
                                <div class="list-group list-group-flush">
                                    <div class="list-group-item">
                                        <strong>✅ Authorization Code Flow:</strong> 成功
                                    </div>
                                    <div class="list-group-item">
                                        <strong>✅ Access Token取得:</strong> 成功
                                    </div>
                                    <div class="list-group-item">
                                        <strong>✅ Token Introspection:</strong> 成功
                                    </div>
                                    <div class="list-group-item">
                                        <strong>✅ API認証:</strong> 成功
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <a href="/" class="btn btn-primary">ホームに戻る</a>
                            <a href="/logout" class="btn btn-outline-secondary">ログアウト</a>
                        </div>
                    </div>
                </div>

                <div class="mt-3 text-center">
                    <small class="text-muted">
                        OAuth 2.0 Test Client - API Test Results
                    </small>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>