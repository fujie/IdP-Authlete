# OpenID Federation Test Client Configuration (Invalid) - Example
# このファイルを .env にコピーして使用してください

# Entity Configuration
# このクライアントは意図的にTrust Anchorに登録されていないEntity IDを使用
# テスト用の固定URLを使用（cloudflared不要）
ENTITY_ID=https://invalid-federation-client.example.com
PORT=3007

# Authorization Server Configuration
AUTHORIZATION_SERVER=http://localhost:3001
REDIRECT_URI=http://localhost:3007/callback
SCOPE=openid profile email

# Trust Anchor Configuration - Trust AnchorのcloudflaredURL
# Valid Clientと同じTrust Anchorを参照するが、このクライアントは登録されていない
TRUST_ANCHOR_ID=https://your-trust-anchor-url.trycloudflare.com

# Federation Configuration
FEDERATION_REGISTRATION_ENDPOINT=http://localhost:3001/federation/registration

# Client Metadata
CLIENT_NAME=Invalid OpenID Federation Test Client
CLIENT_URI=https://invalid-federation-client.example.com
CONTACTS=admin@invalid-federation-client.example.com

# 設定手順:
# 1. TRUST_ANCHOR_IDにTrust AnchorのcloudflaredURLを設定
# 2. または update-federation-urls.sh スクリプトを使用
#
# 注意: このクライアントのEntity IDは意図的にTrust Anchorの
# SUBORDINATE_ENTITIESリストに登録されていません。
# これにより、登録失敗のテストケースを検証できます。
