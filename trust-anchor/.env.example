# Trust Anchor Configuration Example
# このファイルを .env にコピーして使用してください

# Trust Anchor Entity ID (cloudflared URL)
# cloudflaredトンネル起動時に表示されるURLを設定
ENTITY_ID=https://your-trust-anchor-url.trycloudflare.com
PORT=3010

# Subordinate Entities (registered in this trust anchor)
# カンマ区切りで登録するクライアントのEntity IDを指定
# Valid Test ClientのcloudflaredURLを設定
SUBORDINATE_ENTITIES=https://your-valid-client-url.trycloudflare.com

# Trust Anchor Metadata
ORGANIZATION_NAME=OpenID Federation Test Trust Anchor
HOMEPAGE_URI=https://your-trust-anchor-url.trycloudflare.com
CONTACTS=admin@trust-anchor.example.com

# 設定手順:
# 1. cloudflaredトンネルを起動: cloudflared tunnel --url http://localhost:3010
# 2. 表示されたURLをENTITY_IDとHOMEPAGE_URIに設定
# 3. Valid Test ClientのcloudflaredURLをSUBORDINATE_ENTITIESに設定
# 4. または update-federation-urls.sh スクリプトを使用
