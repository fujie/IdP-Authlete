<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Federation Test Client 1 - API Test</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="card">
                    <div class="card-header bg-success text-white">
                        <h1 class="h3 mb-0">🧪 Federation API テスト結果</h1>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-success">
                            <h4>✅ Federation認証成功</h4>
                            <p>動的登録とOpenID Connect認証が正常に完了し、APIテストを実行しました。</p>
                        </div>

                        <!-- Client Registration Info -->
                        <% if (clientRegistration) { %>
                            <h5>クライアント登録情報</h5>
                            <div class="card mb-3">
                                <div class="card-body">
                                    <ul class="list-group list-group-flush">
                                        <li class="list-group-item"><strong>Client ID:</strong> <%= clientRegistration.client_id %></li>
                                        <li class="list-group-item"><strong>登録日時:</strong> <%= new Date(clientRegistration.client_id_issued_at * 1000).toLocaleString() %></li>
                                        <li class="list-group-item"><strong>Trust Chain検証:</strong> 
                                            <span class="badge bg-success">✅ <%= clientRegistration.trust_chain_validation.isValid ? 'Valid' : 'Invalid' %></span>
                                        </li>
                                        <li class="list-group-item"><strong>Trust Anchor:</strong> <%= clientRegistration.trust_chain_validation.trustAnchor %></li>
                                    </ul>
                                </div>
                            </div>
                        <% } %>

                        <!-- Token Introspection Results -->
                        <h5>トークン検証結果</h5>
                        <div class="card mb-3">
                            <div class="card-header">
                                <strong>Introspection Response</strong>
                            </div>
                            <div class="card-body">
                                <pre class="bg-light p-3 rounded"><%= JSON.stringify(introspectionData, null, 2) %></pre>
                            </div>
                        </div>

                        <!-- UserInfo Results -->
                        <% if (userInfoData) { %>
                            <h5>UserInfo結果</h5>
                            <div class="card mb-3">
                                <div class="card-header">
                                    <strong>UserInfo Response</strong>
                                </div>
                                <div class="card-body">
                                    <pre class="bg-light p-3 rounded"><%= JSON.stringify(userInfoData, null, 2) %></pre>
                                </div>
                            </div>
                        <% } %>

                        <!-- Access Token -->
                        <h5>アクセストークン</h5>
                        <div class="mb-3">
                            <textarea class="form-control" rows="3" readonly><%= accessToken %></textarea>
                        </div>

                        <!-- ID Token -->
                        <% if (idToken) { %>
                            <h5>IDトークン</h5>
                            <div class="mb-3">
                                <textarea class="form-control" rows="4" readonly><%= idToken %></textarea>
                            </div>
                        <% } %>

                        <div class="d-grid gap-2">
                            <a href="/" class="btn btn-primary">ホームに戻る</a>
                            <button class="btn btn-secondary" onclick="location.reload()">テストを再実行</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>