# OpenID Federation Test Client Configuration (Valid) - Example
# このファイルを .env にコピーして使用してください

# Entity Configuration - cloudflared public URL
# cloudflaredトンネル起動時に表示されるURLを設定
ENTITY_ID=https://your-valid-client-url.trycloudflare.com
PORT=3006

# Authorization Server Configuration
AUTHORIZATION_SERVER=http://localhost:3001
REDIRECT_URI=http://localhost:3006/callback
SCOPE=openid profile email

# Trust Anchor Configuration - Trust AnchorのcloudflaredURL
TRUST_ANCHOR_ID=https://your-trust-anchor-url.trycloudflare.com

# Federation Configuration
FEDERATION_REGISTRATION_ENDPOINT=http://localhost:3001/federation/registration

# Client Metadata
CLIENT_NAME=Valid OpenID Federation Test Client
CLIENT_URI=https://your-valid-client-url.trycloudflare.com
CONTACTS=admin@your-valid-client-url.trycloudflare.com

# 設定手順:
# 1. cloudflaredトンネルを起動: cloudflared tunnel --url http://localhost:3006
# 2. 表示されたURLをENTITY_ID、CLIENT_URI、CONTACTSに設定
# 3. Trust AnchorのcloudflaredURLをTRUST_ANCHOR_IDに設定
# 4. または update-federation-urls.sh スクリプトを使用
#
# 注意: このクライアントのEntity IDは、Trust Anchorの
# SUBORDINATE_ENTITIESリストに登録されている必要があります
